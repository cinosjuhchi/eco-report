services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:8000"
    volumes:
      - ./:/var/www/html
    command: ["php", "-S", "0.0.0.0:8000", "-t", "/var/www/html"]
    restart: unless-stopped
